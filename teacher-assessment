<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes">
    <title>Judah Wonders Model School · Create Assessment</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;500;600;700;800&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-auth-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-firestore-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-remote-config-compat.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <script type="importmap">{ "imports": { "groq-sdk": "https://esm.run/groq-sdk" } }</script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        :root[data-theme="light"] {
            --bg-primary: #f0f5fa; --bg-sidebar: rgba(255,255,255,0.75); --text-primary: #0b1e33;
            --text-secondary: #2c3e50; --accent: #0A2540; --accent-glow: #C8963E;
            --border: rgba(0,0,0,0.08); --card-bg: rgba(255,255,255,0.7); --hover-bg: rgba(255,255,255,0.9);
            --shadow: 0 8px 20px -5px rgba(0,0,0,0.08); --shadow-lg: 0 15px 35px -10px rgba(0,0,0,0.1);
            --input-bg: #ffffff; --toolbar-bg: #f8fafc;
            --success-bg: rgba(34,197,94,0.15); --success-text: #16a34a;
            --warning-bg: rgba(245,158,11,0.15); --warning-text: #b45309;
            --danger-bg: rgba(239,68,68,0.15); --danger-text: #b91c1c;
            --info-bg: rgba(10,37,64,0.1); --info-text: #0A2540;
            --gradient-start: #0A2540; --gradient-end: #C8963E;
        }
        :root[data-theme="dark"] {
            --bg-primary: #0a0f1e; --bg-sidebar: rgba(15,23,42,0.8); --text-primary: #f0f4fa;
            --text-secondary: #a0b3d9; --accent: #C8963E; --accent-glow: #D4AF37;
            --border: rgba(255,255,255,0.06); --card-bg: rgba(15,23,42,0.6); --hover-bg: rgba(30,41,59,0.8);
            --shadow: 0 8px 20px -5px rgba(0,0,0,0.4); --shadow-lg: 0 15px 35px -10px rgba(0,0,0,0.5);
            --input-bg: rgba(30,41,59,0.9); --toolbar-bg: #1e293b;
            --success-bg: rgba(34,197,94,0.15); --success-text: #4ade80;
            --warning-bg: rgba(245,158,11,0.15); --warning-text: #fbbf24;
            --danger-bg: rgba(239,68,68,0.15); --danger-text: #f87171;
            --info-bg: rgba(200,150,62,0.15); --info-text: #D4AF37;
            --gradient-start: #0A2540; --gradient-end: #C8963E;
        }

        body { font-family: 'Inter', sans-serif; background: var(--bg-primary); color: var(--text-primary); min-height: 100vh; display: flex; position: relative; overflow-x: hidden; }
        .bg-logo { position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 400px; max-width: 70vw; opacity: 0.03; pointer-events: none; animation: breathe 6s ease-in-out infinite; }
        @keyframes breathe { 0%,100% { transform: translate(-50%, -50%) scale(1); opacity: 0.03; } 50% { transform: translate(-50%, -50%) scale(1.06); opacity: 0.06; } }
        .orb { position: fixed; width: 250px; height: 250px; border-radius: 50%; filter: blur(80px); pointer-events: none; animation: float 25s infinite alternate; }
        .orb-1 { top: -100px; left: -100px; background: radial-gradient(circle at 30% 30%, rgba(10,37,64,0.12), transparent 70%); }
        .orb-2 { bottom: -150px; right: -50px; width: 350px; height: 350px; background: radial-gradient(circle at 70% 70%, rgba(200,150,62,0.12), transparent); animation-duration: 30s; }
        @keyframes float { 0% { transform: translate(0,0) scale(1); } 100% { transform: translate(40px,30px) scale(1.15); } }

        .sidebar { width: 260px; background: var(--bg-sidebar); backdrop-filter: blur(16px); border-right: 1px solid var(--border); padding: 1.5rem 0.8rem; display: flex; flex-direction: column; transition: width 0.3s; position: fixed; top: 0; left: 0; height: 100vh; overflow-y: auto; z-index: 100; box-shadow: var(--shadow); }
        .sidebar.collapsed { width: 85px; }
        .sidebar .logo-area { display: flex; align-items: center; gap: 0.8rem; margin-bottom: 1.8rem; padding: 0 0.3rem; text-decoration: none; }
        .sidebar .logo-area img { width: 38px; height: 38px; object-fit: contain; animation: logoPulse 3s ease-in-out infinite; }
        @keyframes logoPulse { 0%,100%{transform:scale(1);} 50%{transform:scale(1.05);} }
        .sidebar .logo-area h2 { font-family: 'Playfair Display', serif; font-size: 1.1rem; font-weight: 700; white-space: nowrap; color: var(--accent); line-height: 1.2; }
        .sidebar .logo-area h2 span { display: block; font-size: 0.6rem; letter-spacing: 1.5px; color: var(--accent-glow); }
        .sidebar.collapsed .logo-area h2 { display: none; }
        .sidebar .nav-item { display: flex; align-items: center; gap: 0.9rem; padding: 0.7rem 1rem; border-radius: 1rem; color: var(--text-secondary); transition: all 0.2s; cursor: pointer; margin-bottom: 0.25rem; text-decoration: none; font-weight: 500; white-space: nowrap; border: 1px solid transparent; font-size: 0.9rem; }
        .sidebar .nav-item:hover { background: var(--hover-bg); color: var(--accent); border-color: var(--border); transform: translateX(3px); }
        .sidebar .nav-item.active { background: linear-gradient(135deg, var(--accent), var(--accent-glow)); color: white; box-shadow: 0 6px 15px -5px var(--accent); }
        .sidebar .nav-item i { font-size: 1.2rem; width: 1.6rem; text-align: center; }
        .sidebar.collapsed .nav-item span { display: none; }
        .sidebar.collapsed .nav-item { justify-content: center; padding: 0.7rem; }
        .sidebar .toggle-btn { margin-top: auto; display: flex; justify-content: flex-end; padding: 0.8rem 0.5rem; cursor: pointer; color: var(--text-secondary); font-size: 1.1rem; }

        .main-content { flex: 1; margin-left: 260px; padding: 1.5rem; transition: margin-left 0.3s; max-width: calc(100vw - 260px); position: relative; z-index: 5; }
        .main-content.expanded { margin-left: 85px; max-width: calc(100vw - 85px); }

        .top-bar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; flex-wrap: wrap; gap: 1rem; }
        .page-title h1 { font-family: 'Playfair Display', serif; font-size: 1.8rem; font-weight: 700; color: var(--accent); margin-bottom: 0.1rem; }
        .page-title p { color: var(--text-secondary); font-size: 0.85rem; }
        .profile-area { display: flex; align-items: center; gap: 1rem; }
        .theme-toggle { background: var(--card-bg); border: 1px solid var(--border); border-radius: 2rem; padding: 0.4rem 1rem; display: flex; align-items: center; gap: 0.4rem; cursor: pointer; color: var(--text-secondary); font-size: 0.8rem; transition: 0.2s; }
        .profile-pic { width: 36px; height: 36px; border-radius: 50%; background: linear-gradient(135deg, var(--accent), var(--accent-glow)); display: flex; align-items: center; justify-content: center; color: white; font-weight: 600; font-size: 0.9rem; box-shadow: 0 4px 10px var(--accent); overflow: hidden; }
        .profile-pic img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
        .dropdown-menu { position: absolute; right: 0; top: 50px; background: var(--bg-sidebar); border: 1px solid var(--border); border-radius: 1rem; padding: 0.4rem; min-width: 160px; display: none; z-index: 1000; }
        .profile-dropdown { position: relative; cursor: pointer; }
        .profile-dropdown:hover .dropdown-menu { display: block; animation: fadeIn 0.2s; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(-10px); } to { opacity: 1; transform: translateY(0); } }
        .dropdown-item { padding: 0.6rem 1rem; color: var(--text-primary); border-radius: 0.8rem; display: flex; align-items: center; gap: 0.6rem; font-size: 0.85rem; cursor: pointer; }
        .dropdown-item:hover { background: var(--hover-bg); color: var(--accent); }

        .type-selector { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-bottom: 2rem; }
        .type-card { background: var(--card-bg); border: 2px solid var(--border); border-radius: 1.5rem; padding: 2rem 1.5rem; text-align: center; cursor: pointer; transition: all 0.3s; }
        .type-card:hover { transform: translateY(-5px); box-shadow: var(--shadow-lg); }
        .type-card.active { border-color: var(--accent); background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end)); color: white; }
        .type-card.active .type-icon { color: white; }
        .type-card .type-icon { font-size: 3rem; color: var(--accent); margin-bottom: 1rem; transition: color 0.3s; }
        .type-card .type-label { font-weight: 700; font-size: 1.2rem; margin-bottom: 0.5rem; }
        .type-card .type-desc { color: var(--text-secondary); font-size: 0.85rem; }
        .type-card.active .type-desc { color: rgba(255,255,255,0.8); }

        .card { background: var(--card-bg); border: 1px solid var(--border); border-radius: 1.5rem; padding: 1.5rem; box-shadow: var(--shadow); margin-bottom: 1.5rem; }
        .card-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; flex-wrap: wrap; gap: 1rem; padding-bottom: 1rem; border-bottom: 1px solid var(--border); }
        .card-header h3 { font-size: 1.2rem; font-weight: 600; display: flex; align-items: center; gap: 0.5rem; }
        .form-group { margin-bottom: 1.25rem; }
        .form-group label { display: block; margin-bottom: 0.5rem; color: var(--text-secondary); font-weight: 500; font-size: 0.9rem; }
        .form-control { width: 100%; padding: 0.75rem 1rem; background: var(--input-bg); border: 1px solid var(--border); border-radius: 1rem; color: var(--text-primary); font-size: 0.95rem; font-family: inherit; outline: none; transition: border-color 0.2s; }
        .form-control:focus { border-color: var(--accent); }
        textarea.form-control { min-height: 100px; resize: vertical; }
        select.form-control { appearance: auto; }
        .btn { padding: 0.6rem 1.3rem; border: none; border-radius: 2rem; font-weight: 500; font-size: 0.85rem; cursor: pointer; transition: 0.2s; display: inline-flex; align-items: center; gap: 0.4rem; white-space: nowrap; font-family: inherit; }
        .btn-primary { background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end)); color: white; }
        .btn-success { background: var(--success-text); color: white; }
        .btn-danger { background: var(--danger-text); color: white; }
        .btn-outline { background: transparent; border: 1px solid var(--border); color: var(--text-primary); }
        .btn-outline:hover { background: var(--hover-bg); border-color: var(--accent); }
        .btn-sm { padding: 0.35rem 0.9rem; font-size: 0.75rem; border-radius: 1.5rem; }
        .btn:hover { opacity: 0.9; transform: translateY(-1px); }
        .btn-group { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 1.5rem; justify-content: flex-end; }

        .weeks-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 0.75rem; margin-top: 0.75rem; }
        .week-checkbox { display: flex; align-items: center; gap: 0.75rem; padding: 0.75rem; background: var(--card-bg); border: 1px solid var(--border); border-radius: 0.75rem; cursor: pointer; transition: all 0.2s; }
        .week-checkbox:hover { background: var(--hover-bg); border-color: var(--accent); }
        .week-checkbox input { width: 1.2rem; height: 1.2rem; cursor: pointer; accent-color: var(--accent); }
        .week-checkbox span { font-size: 0.85rem; }

        .question-item { background: var(--hover-bg); border-radius: 1rem; padding: 1rem; margin-bottom: 1rem; border: 1px solid var(--border); }
        .question-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.75rem; flex-wrap: wrap; gap: 0.5rem; }
        .question-text { font-weight: 600; font-size: 0.95rem; flex: 1; }
        .question-options { display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.5rem; margin: 0.75rem 0; }
        .question-option { display: flex; align-items: center; gap: 0.5rem; font-size: 0.9rem; }
        .correct-badge { background: var(--success-bg); color: var(--success-text); padding: 0.2rem 0.8rem; border-radius: 2rem; font-size: 0.75rem; display: inline-block; }
        .difficulty-badge { padding: 0.2rem 0.6rem; border-radius: 1rem; font-size: 0.7rem; display: inline-block; }
        .difficulty-easy { background: var(--success-bg); color: var(--success-text); }
        .difficulty-medium { background: var(--warning-bg); color: var(--warning-text); }
        .difficulty-hard { background: var(--danger-bg); color: var(--danger-text); }
        .type-badge-sm { padding: 0.2rem 0.6rem; border-radius: 1rem; font-size: 0.7rem; background: var(--info-bg); color: var(--info-text); display: inline-block; }

        .info-box { background: var(--info-bg); border-left: 4px solid var(--accent); padding: 1rem; border-radius: 1rem; margin-bottom: 1rem; font-size: 0.85rem; display: flex; align-items: center; gap: 0.75rem; }

        .upload-area { border: 2px dashed var(--border); border-radius: 1rem; padding: 2rem; text-align: center; cursor: pointer; transition: all 0.2s; margin-bottom: 1rem; }
        .upload-area:hover { border-color: var(--accent); background: var(--hover-bg); }
        .upload-area i { font-size: 3rem; color: var(--accent); margin-bottom: 1rem; display: block; }
        .csv-preview { max-height: 300px; overflow-y: auto; margin-top: 1rem; }
        .csv-preview table { font-size: 0.7rem; width: 100%; border-collapse: collapse; }
        .csv-preview th, .csv-preview td { padding: 0.3rem; border: 1px solid var(--border); }

        .settings-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1rem; }
        .checkbox-item { display: flex; align-items: center; gap: 0.75rem; padding: 0.5rem; background: var(--input-bg); border-radius: 0.75rem; }
        .checkbox-item input { width: 1.2rem; height: 1.2rem; accent-color: var(--accent); }

        .modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); backdrop-filter: blur(5px); display: none; justify-content: center; align-items: center; z-index: 2000; }
        .modal-overlay.active { display: flex; }
        .modal-content { background: var(--card-bg); border: 1px solid var(--border); border-radius: 1.5rem; padding: 1.5rem; max-width: 800px; width: 90%; max-height: 85vh; overflow-y: auto; box-shadow: var(--shadow-lg); }
        .modal-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; }
        .modal-header h2 { font-size: 1.3rem; }
        .modal-close { font-size: 1.5rem; cursor: pointer; color: var(--text-secondary); transition: 0.2s; }
        .modal-close:hover { color: var(--danger-text); }
        .ai-chat { height: 300px; overflow-y: auto; padding: 1rem; background: var(--hover-bg); border-radius: 1rem; margin-bottom: 1rem; display: flex; flex-direction: column; gap: 0.75rem; }
        .ai-message { padding: 0.75rem 1rem; border-radius: 1rem; max-width: 85%; font-size: 0.9rem; line-height: 1.5; }
        .ai-message.user { background: var(--accent); color: white; align-self: flex-end; }
        .ai-message.assistant { background: var(--card-bg); color: var(--text-primary); border: 1px solid var(--border); align-self: flex-start; }

        .loading-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.9); display: none; justify-content: center; align-items: center; z-index: 3000; }
        .loading-overlay.active { display: flex; }
        .loading-content { background: var(--card-bg); padding: 2rem; border-radius: 1.5rem; text-align: center; }
        .loading-content i { font-size: 3rem; color: var(--accent); margin-bottom: 1rem; }

        .toast-container { position: fixed; top: 1rem; right: 1rem; z-index: 5000; display: flex; flex-direction: column; gap: 0.5rem; }
        .toast { background: var(--card-bg); backdrop-filter: blur(16px); border: 1px solid var(--border); border-radius: 1rem; padding: 1rem 1.5rem; min-width: 300px; max-width: 450px; box-shadow: var(--shadow-lg); display: flex; align-items: flex-start; gap: 0.8rem; animation: slideInRight 0.3s; }
        .toast.removing { animation: slideOutRight 0.3s ease-in forwards; }
        .toast-icon { font-size: 1.2rem; margin-top: 2px; }
        .toast-icon.success { color: var(--success-text); }
        .toast-icon.error { color: var(--danger-text); }
        .toast-icon.info { color: var(--info-text); }
        @keyframes slideInRight { from { transform: translateX(120%); opacity: 0; } to { transform: translateX(0); opacity: 1; } }
        @keyframes slideOutRight { from { transform: translateX(0); opacity: 1; } to { transform: translateX(120%); opacity: 0; } }

        @media (max-width: 768px) {
            .sidebar { width: 85px; }
            .sidebar .logo-area h2, .sidebar .nav-item span { display: none; }
            .main-content { margin-left: 85px; max-width: calc(100vw - 85px); padding: 1rem; }
            .type-selector { grid-template-columns: 1fr; }
            .question-options { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <img src="https://res.cloudinary.com/dda7kpir8/image/upload/q_auto/f_auto/v1781038310/ChatGPT_Image_Jun_9_2026_09_25_06_AM_elnine.png" alt="" class="bg-logo" aria-hidden="true">
    <div class="orb orb-1"></div><div class="orb orb-2"></div>
    <div class="toast-container" id="toastContainer"></div>

    <!-- SIDEBAR -->
    <div class="sidebar" id="sidebar">
        <a href="index.html" class="logo-area">
            <img src="https://res.cloudinary.com/dda7kpir8/image/upload/q_auto/f_auto/v1781038310/ChatGPT_Image_Jun_9_2026_09_25_06_AM_elnine.png" alt="JWMS Logo">
            <h2>Judah Wonders<span>Model School</span></h2>
        </a>
        <a href="teacher-dashboard.html" class="nav-item"><i class="fas fa-chart-pie"></i><span>Dashboard</span></a>
        <a href="teacher-workspace.html" class="nav-item"><i class="fas fa-book-open"></i><span>Workspace</span></a>
        <a href="teacher-assessments.html" class="nav-item active"><i class="fas fa-pen-fancy"></i><span>Create Assessment</span></a>
        <a href="teacher-assessment-history.html" class="nav-item"><i class="fas fa-history"></i><span>History</span></a>
        <a href="teacher-profile.html" class="nav-item"><i class="fas fa-user-edit"></i><span>Profile</span></a>
        <div class="toggle-btn" id="sidebarToggle"><i class="fas fa-chevron-left"></i></div>
    </div>

    <!-- MAIN CONTENT -->
    <div class="main-content" id="mainContent">
        <div class="top-bar">
            <div class="page-title">
                <h1>Create Assessment</h1>
                <p>Build tests, assignments, and exams with AI assistance</p>
            </div>
            <div class="profile-area">
                <div class="theme-toggle" id="themeToggle"><i class="fas fa-sun"></i><span>Light</span><i class="fas fa-moon"></i></div>
                <div class="profile-dropdown">
                    <div class="profile-pic" id="profilePic">TE</div>
                    <div class="dropdown-menu"><div class="dropdown-item" id="logoutBtn"><i class="fas fa-sign-out-alt"></i> Logout</div></div>
                </div>
            </div>
        </div>

        <!-- TYPE SELECTOR -->
        <div class="type-selector" id="typeSelector">
            <div class="type-card active" data-type="test" onclick="selectType('test', this)">
                <div class="type-icon"><i class="fas fa-laptop"></i></div>
                <div class="type-label">CBT Test</div>
                <div class="type-desc">Multiple choice, true/false, fill‑in‑blank. Auto‑graded with instant results.</div>
            </div>
            <div class="type-card" data-type="assignment" onclick="selectType('assignment', this)">
                <div class="type-icon"><i class="fas fa-tasks"></i></div>
                <div class="type-label">Assignment</div>
                <div class="type-desc">Essay, file upload, project work. Manual grading with feedback.</div>
            </div>
            <div class="type-card" data-type="exam" onclick="selectType('exam', this)">
                <div class="type-icon"><i class="fas fa-pen-fancy"></i></div>
                <div class="type-label">Examination</div>
                <div class="type-desc">Timed formal exam. Strict window, question bank, auto or manual grade.</div>
            </div>
        </div>

        <!-- FORM -->
        <form id="assessmentForm">
            <div class="card">
                <div class="card-header">
                    <h3><i class="fas fa-info-circle"></i> <span id="formTitle">Test</span> Information</h3>
                    <div>
                        <button type="button" class="btn btn-outline" id="useAIBtn"><i class="fas fa-robot"></i> AI Generate Questions</button>
                        <button type="button" class="btn btn-outline" id="bulkUploadBtn"><i class="fas fa-upload"></i> Bulk Upload CSV</button>
                        <button type="button" class="btn btn-outline" id="downloadTemplateBtn"><i class="fas fa-download"></i> CSV Template</button>
                    </div>
                </div>

                <div class="info-box">
                    <i class="fas fa-lightbulb"></i>
                    <span><strong>Pro Tip:</strong> Select your subject and weeks below, then use <strong>AI Generate</strong> to create questions directly from your approved lesson notes and syllabus.</span>
                </div>

                <div class="form-group">
                    <label><i class="fas fa-book"></i> Subject *</label>
                    <select id="subject" class="form-control" required>
                        <option value="">Select a subject...</option>
                    </select>
                </div>

                <div class="form-group" id="weeksSection">
                    <label><i class="fas fa-calendar-week"></i> Weeks Covered <span style="color:var(--text-secondary);font-weight:400;">(Select all that apply)</span></label>
                    <div id="weeksCheckboxes" class="weeks-grid">
                        <p style="grid-column:1/-1;text-align:center;padding:2rem;color:var(--text-secondary);">
                            <i class="fas fa-arrow-up"></i> Select a subject to load weeks from your approved syllabus
                        </p>
                    </div>
                </div>

                <div class="form-group">
                    <label><i class="fas fa-heading"></i> Title *</label>
                    <input type="text" id="title" class="form-control" required placeholder="e.g., First Term Mathematics Examination">
                </div>

                <div class="form-group">
                    <label><i class="fas fa-align-left"></i> Instructions / Description</label>
                    <textarea id="instructions" class="form-control" rows="3" placeholder="Provide clear instructions for students..."></textarea>
                </div>

                <!-- DYNAMIC SETTINGS -->
                <div id="dynamicSettings" class="settings-grid"></div>
            </div>

            <!-- QUESTIONS SECTION -->
            <div class="card" id="questionsCard">
                <div class="card-header">
                    <h3><i class="fas fa-question-circle"></i> Questions <span id="questionCount" style="font-size:0.8rem;color:var(--text-secondary);">(0)</span></h3>
                    <div style="display:flex;gap:0.5rem;flex-wrap:wrap;" id="questionTypeButtons">
                        <button type="button" class="btn btn-outline btn-sm active" data-qtype="mcq">MCQ</button>
                        <button type="button" class="btn btn-outline btn-sm" data-qtype="truefalse">True/False</button>
                        <button type="button" class="btn btn-outline btn-sm" data-qtype="fillblank">Fill Blank</button>
                        <button type="button" class="btn btn-outline btn-sm" data-qtype="theory">Theory/Essay</button>
                    </div>
                </div>
                <div id="questionsList"><p style="text-align:center;color:var(--text-secondary);padding:2rem;">No questions yet. Click "Add Question", use AI, or upload CSV.</p></div>
                <button type="button" class="btn btn-outline" id="addQuestionBtn" style="margin-top:1rem;"><i class="fas fa-plus"></i> Add Question Manually</button>
            </div>

            <div class="btn-group">
                <button type="button" class="btn btn-outline" id="saveDraftBtn"><i class="fas fa-save"></i> Save as Draft</button>
                <button type="button" class="btn btn-primary" id="publishBtn"><i class="fas fa-paper-plane"></i> Publish Assessment</button>
            </div>
        </form>
    </div>

    <!-- AI MODAL -->
    <div class="modal-overlay" id="aiModal">
        <div class="modal-content">
            <div class="modal-header"><h2><i class="fas fa-robot"></i> AI Question Generator</h2><i class="fas fa-times modal-close" onclick="closeAIModal()"></i></div>
            <div id="aiChat" class="ai-chat"><div class="ai-message assistant">👋 Hello! I'll generate questions based on your approved lesson notes. Select subject, weeks, and preferences below, then click <strong>Generate</strong>.</div></div>
            <div class="form-group"><label>Number of Questions</label><input type="number" id="aiCount" class="form-control" value="10" min="1" max="50"></div>
            <div class="form-group"><label>Difficulty</label><select id="aiDifficulty" class="form-control"><option value="easy">Easy</option><option value="medium" selected>Medium</option><option value="hard">Hard</option><option value="mixed">Mixed</option></select></div>
            <div class="form-group"><label>Question Type</label><select id="aiType" class="form-control"><option value="mcq">Multiple Choice</option><option value="truefalse">True/False</option><option value="fillblank">Fill in the Blank</option><option value="mixed">Mixed</option></select></div>
            <div class="btn-group" style="justify-content:flex-start;">
                <button class="btn btn-primary" onclick="generateFromLessonNotes()"><i class="fas fa-book-open"></i> Generate from Lesson Notes</button>
                <button class="btn btn-success" onclick="insertAIGenerated()"><i class="fas fa-plus-circle"></i> Insert Generated Questions</button>
            </div>
        </div>
    </div>

    <!-- BULK UPLOAD MODAL -->
    <div class="modal-overlay" id="bulkModal">
        <div class="modal-content" style="max-width:900px;"><div class="modal-header"><h2><i class="fas fa-upload"></i> Bulk Upload CSV</h2><i class="fas fa-times modal-close" onclick="closeBulkModal()"></i></div>
            <div class="info-box"><i class="fas fa-info-circle"></i><span>CSV format: <code>Question, Option A, Option B, Option C, Option D, Correct (A/B/C/D), Points</code></span></div>
            <div class="upload-area" id="uploadArea"><i class="fas fa-cloud-upload-alt"></i><h4>Click or drag CSV file here</h4><input type="file" id="csvFile" accept=".csv" style="display:none;"></div>
            <div id="csvPreview" class="csv-preview" style="display:none;"><h4>Preview:</h4><table id="previewTable"></table></div>
            <div class="form-group" style="margin-top:1rem;"><label>Default Points</label><input type="number" id="defaultPoints" class="form-control" value="1" min="1"></div>
            <div class="btn-group"><button class="btn btn-outline" onclick="closeBulkModal()">Cancel</button><button class="btn btn-primary" id="importCsvBtn">Import Questions</button></div>
        </div>
    </div>

    <!-- LOADING -->
    <div class="loading-overlay" id="loadingOverlay"><div class="loading-content"><i class="fas fa-spinner fa-spin fa-2x"></i><h3 style="margin-top:1rem;">Processing...</h3></div></div>

    <script type="module">
        // ==================== FIREBASE ====================
        const firebaseConfig = {
            apiKey: "AIzaSyADXHEiaSQ146QsGsaOCElARtpwrLE8svs",
            authDomain: "judah-c737e.firebaseapp.com",
            projectId: "judah-c737e",
            storageBucket: "judah-c737e.firebasestorage.app",
            messagingSenderId: "739196063078",
            appId: "1:739196063078:web:c0ee1c1da2cc3794ca8ca7",
            measurementId: "G-YEM22F1C39"
        };
        firebase.initializeApp(firebaseConfig);
        const auth = firebase.auth();
        const db = firebase.firestore();
        const remoteConfig = firebase.remoteConfig();
        remoteConfig.settings.minimumFetchIntervalMillis = 0;
        import Groq from "groq-sdk";

        // ==================== GLOBAL STATE ====================
        let currentUser = null, userData = null, currentType = 'test';
        let subjects = [], currentSubject = null, questions = [], selectedWeeks = [];
        let currentQuestionType = 'mcq', groq = null;
        let lastGenerated = [], csvData = [], syllabusWeeks = [];

        // ==================== HELPERS ====================
        function showToast(title, message, type = 'info') {
            const icons = { success: 'fa-check-circle', error: 'fa-exclamation-circle', info: 'fa-info-circle' };
            const container = document.getElementById('toastContainer');
            const toast = document.createElement('div'); toast.className = 'toast';
            toast.innerHTML = `<i class="fas ${icons[type]} toast-icon ${type}"></i><div class="toast-content"><div class="toast-title">${title}</div><div class="toast-message">${message}</div></div><button class="toast-close" onclick="this.parentElement.remove()"><i class="fas fa-times"></i></button>`;
            container.appendChild(toast);
            setTimeout(() => { toast.classList.add('removing'); setTimeout(() => toast.remove(), 300); }, 4000);
        }
        function showLoading() { document.getElementById('loadingOverlay').classList.add('active'); }
        function hideLoading() { document.getElementById('loadingOverlay').classList.remove('active'); }
        function escapeHtml(s) { const d = document.createElement('div'); d.textContent = s || ''; return d.innerHTML; }

        // ==================== SIDEBAR & THEME ====================
        const sidebar = document.getElementById('sidebar'), mainContent = document.getElementById('mainContent');
        document.getElementById('sidebarToggle').addEventListener('click', () => {
            sidebar.classList.toggle('collapsed'); mainContent.classList.toggle('expanded');
            const icon = document.getElementById('sidebarToggle').querySelector('i');
            icon.classList.toggle('fa-chevron-left'); icon.classList.toggle('fa-chevron-right');
        });
        const html = document.documentElement, themeToggle = document.getElementById('themeToggle');
        const savedTheme = localStorage.getItem('judah-theme') || 'dark';
        html.setAttribute('data-theme', savedTheme);
        themeToggle.querySelector('span').textContent = savedTheme === 'dark' ? 'Light' : 'Dark';
        themeToggle.addEventListener('click', () => {
            const next = html.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
            html.setAttribute('data-theme', next); localStorage.setItem('judah-theme', next);
            themeToggle.querySelector('span').textContent = next === 'dark' ? 'Light' : 'Dark';
        });

        // ==================== TYPE SELECTION ====================
        window.selectType = (type, card) => {
            currentType = type;
            document.querySelectorAll('.type-card').forEach(c => c.classList.remove('active'));
            card.classList.add('active');
            document.getElementById('formTitle').textContent = type === 'test' ? 'Test' : type === 'assignment' ? 'Assignment' : 'Examination';
            document.getElementById('questionsCard').style.display = type === 'assignment' ? 'none' : 'block';
            document.getElementById('weeksSection').style.display = (type === 'test' || type === 'exam') ? 'block' : 'none';
            renderDynamicSettings();
        };

        function renderDynamicSettings() {
            const div = document.getElementById('dynamicSettings');
            if (currentType === 'test') {
                div.innerHTML = `
                    <div class="form-group"><label><i class="fas fa-clock"></i> Duration (minutes)</label><input type="number" id="duration" class="form-control" value="30" min="1" max="180"></div>
                    <div class="form-group"><label><i class="fas fa-random"></i> Randomize</label><div class="checkbox-item"><input type="checkbox" id="randomizeQuestions" checked><span>Randomize question order</span></div></div>
                    <div class="form-group"><label><i class="fas fa-eye"></i> Grading</label><div class="checkbox-item"><input type="checkbox" id="autoGrade" checked><span>Auto‑grade on submission</span></div></div>`;
            } else if (currentType === 'assignment') {
                div.innerHTML = `
                    <div class="form-group"><label><i class="fas fa-calendar-alt"></i> Due Date *</label><input type="date" id="dueDate" class="form-control" required></div>
                    <div class="form-group"><label><i class="fas fa-star"></i> Total Marks</label><input type="number" id="totalMarks" class="form-control" value="20" min="1"></div>
                    <div class="form-group"><label><i class="fas fa-upload"></i> Submission Type</label><select id="submissionType" class="form-control"><option value="text">Text Entry</option><option value="file">File Upload</option><option value="both">Both</option></select></div>`;
            } else {
                div.innerHTML = `
                    <div class="form-group"><label><i class="fas fa-clock"></i> Duration (minutes)</label><input type="number" id="duration" class="form-control" value="120" min="1" max="300"></div>
                    <div class="form-group"><label><i class="fas fa-calendar-alt"></i> Start Date/Time</label><input type="datetime-local" id="startDate" class="form-control"></div>
                    <div class="form-group"><label><i class="fas fa-calendar-check"></i> End Date/Time</label><input type="datetime-local" id="endDate" class="form-control"></div>
                    <div class="form-group"><label><i class="fas fa-random"></i> Randomize</label><div class="checkbox-item"><input type="checkbox" id="randomizeQuestions" checked><span>Randomize question order</span></div></div>
                    <div class="form-group"><label><i class="fas fa-eye"></i> Grading</label><div class="checkbox-item"><input type="checkbox" id="autoGrade" checked><span>Auto‑grade MCQs</span></div></div>`;
            }
        }

        // ==================== QUESTION TYPE BUTTONS ====================
        document.getElementById('questionTypeButtons').addEventListener('click', (e) => {
            const btn = e.target.closest('.btn');
            if (!btn) return;
            document.querySelectorAll('#questionTypeButtons .btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active'); currentQuestionType = btn.dataset.qtype;
        });

        // ==================== ADD / RENDER / EDIT / REMOVE QUESTIONS ====================
        document.getElementById('addQuestionBtn').addEventListener('click', () => {
            questions.push({ type: currentQuestionType, question: '', options: ['','','',''], correct: currentQuestionType==='mcq'?'A':currentQuestionType==='truefalse'?'True':'', points: 1, difficulty: 'medium' });
            renderQuestions(); editQuestion(questions.length - 1);
        });

        function renderQuestions() {
            const container = document.getElementById('questionsList');
            document.getElementById('questionCount').textContent = `(${questions.length})`;
            if (!questions.length) { container.innerHTML = '<p style="text-align:center;color:var(--text-secondary);padding:2rem;">No questions yet. Click "Add Question", use AI, or upload CSV.</p>'; return; }
            container.innerHTML = questions.map((q, i) => {
                let opts = '';
                if (q.type === 'mcq') {
                    opts = `<div class="question-options">${['A','B','C','D'].map(l => `<div class="question-option"><input type="radio" name="correct_${i}" value="${l}" ${q.correct===l?'checked':''} onchange="window.setCorrect(${i},'${l}')"><span>${l}. ${escapeHtml(q.options[l.charCodeAt(0)-65]||'')}</span></div>`).join('')}</div>`;
                } else if (q.type === 'truefalse') {
                    opts = `<div class="question-options"><div class="question-option"><input type="radio" name="correct_${i}" value="True" ${q.correct==='True'?'checked':''} onchange="window.setCorrect(${i},'True')"><span>True</span></div><div class="question-option"><input type="radio" name="correct_${i}" value="False" ${q.correct==='False'?'checked':''} onchange="window.setCorrect(${i},'False')"><span>False</span></div></div>`;
                } else if (q.type === 'fillblank') {
                    opts = `<div class="form-group"><input type="text" class="form-control" placeholder="Correct answer" value="${escapeHtml(q.correct||'')}" onchange="window.setCorrect(${i},this.value)"></div>`;
                } else if (q.type === 'theory') {
                    opts = `<div class="form-group"><textarea class="form-control" rows="3" placeholder="Model answer / marking guide" onchange="window.setCorrect(${i},this.value)">${escapeHtml(q.correct||'')}</textarea></div>`;
                }
                return `<div class="question-item">
                    <div class="question-header">
                        <span class="question-text">Q${i+1}: ${escapeHtml(q.question) || '<em>Click edit to add text</em>'}</span>
                        <div style="display:flex;gap:0.4rem;align-items:center;">
                            <span class="type-badge-sm">${q.type}</span><span class="difficulty-badge difficulty-${q.difficulty}">${q.difficulty}</span>
                            <span class="correct-badge">${q.points} pt${q.points>1?'s':''}</span>
                            <button type="button" class="btn btn-sm btn-outline" onclick="window.editQuestion(${i})"><i class="fas fa-edit"></i></button>
                            <button type="button" class="btn btn-sm btn-danger" onclick="window.removeQuestion(${i})"><i class="fas fa-trash"></i></button>
                        </div>
                    </div>${opts}</div>`;
            }).join('');
        }

        window.setCorrect = (i, val) => { questions[i].correct = val; };
        window.removeQuestion = (i) => { questions.splice(i, 1); renderQuestions(); };
        window.editQuestion = (i) => {
            const q = questions[i];
            const newQ = prompt('Question text:', q.question); if (newQ !== null) q.question = newQ;
            if (q.type === 'mcq') for (let j=0;j<4;j++) { const o = prompt(`Option ${String.fromCharCode(65+j)}:`, q.options[j]); if (o !== null) q.options[j] = o; }
            const pts = prompt('Points:', q.points); if (pts !== null) q.points = parseInt(pts) || 1;
            const diff = prompt('Difficulty (easy/medium/hard):', q.difficulty); if (diff && ['easy','medium','hard'].includes(diff)) q.difficulty = diff;
            renderQuestions();
        };

        // ==================== SUBJECT & WEEKS ====================
        document.getElementById('subject').addEventListener('change', async (e) => {
            currentSubject = e.target.value; selectedWeeks = [];
            if (!currentSubject) return;
            const snap = await db.collection('syllabus').where('teacherId','==',auth.currentUser.uid).where('subject','==',currentSubject).where('status','==','approved').get();
            syllabusWeeks = []; snap.forEach(d => (d.data().weeks||[]).forEach(w => syllabusWeeks.push({...w, syllabusId: d.id})));
            document.getElementById('weeksCheckboxes').innerHTML = syllabusWeeks.length ? syllabusWeeks.map(w => `<label class="week-checkbox"><input type="checkbox" value="${w.week}" onchange="window.toggleWeek(this)"><span><strong>Week ${w.week}</strong>: ${w.topic||'No topic'}</span></label>`).join('') : '<p style="grid-column:1/-1;text-align:center;padding:1rem;color:var(--warning-text);">No approved syllabus weeks found. Create and approve a syllabus first.</p>';
        });
        window.toggleWeek = (cb) => { const w = parseInt(cb.value); if (cb.checked) { if (!selectedWeeks.includes(w)) selectedWeeks.push(w); } else { selectedWeeks = selectedWeeks.filter(x => x !== w); } selectedWeeks.sort((a,b)=>a-b); };

        // ==================== AI GENERATION ====================
        async function getLessonNoteContent() {
            if (!currentSubject || !selectedWeeks.length) return null;
            const snap = await db.collection('lessonNotes').where('teacherId','==',auth.currentUser.uid).where('subject','==',currentSubject).where('week','in',selectedWeeks).where('status','==','approved').get();
            const notes = []; snap.forEach(d => { const data = d.data(); notes.push({ week: data.week, topic: data.topic, content: data.lessonNote || data.lessonContent || '' }); });
            return notes.length ? notes : null;
        }
        window.generateFromLessonNotes = async () => {
            if (!groq) { showToast('AI Not Ready', 'AI service unavailable. Set Groq API key in Remote Config.', 'error'); return; }
            if (!currentSubject || !selectedWeeks.length) { showToast('Selection Required', 'Select a subject and at least one week.', 'error'); return; }
            const count = parseInt(document.getElementById('aiCount').value) || 10, difficulty = document.getElementById('aiDifficulty').value, type = document.getElementById('aiType').value;
            const chatDiv = document.getElementById('aiChat');
            chatDiv.innerHTML += `<div class="ai-message user">Generating ${count} questions from lesson notes...</div>`;
            const thinking = document.createElement('div'); thinking.className = 'ai-message assistant'; thinking.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Reading your lesson notes...'; chatDiv.appendChild(thinking);
            showLoading();
            try {
                const notes = await getLessonNoteContent();
                if (!notes) { chatDiv.removeChild(thinking); chatDiv.innerHTML += `<div class="ai-message assistant">❌ No approved lesson notes found for the selected weeks.</div>`; hideLoading(); return; }
                let context = notes.map(n => `WEEK ${n.week}: ${n.topic}\n${n.content?.substring(0,1500)||''}`).join('\n\n');
                const typeMap = { mcq: 'multiple choice with 4 options (A-D)', truefalse: 'true/false', fillblank: 'fill-in-the-blank', mixed: 'a mix of MCQ, true/false, and fill-in-the-blank' };
                const prompt = `Generate ${count} ${difficulty} ${typeMap[type]} questions based on:\n\n${context}\n\nReturn ONLY a valid JSON array. MCQ: [{"question":"...","options":["A","B","C","D"],"correct":"A","points":1}]. True/False: [{"question":"...","correct":"True","points":1}]. Fill-blank: [{"question":"...with ______","correct":"answer","points":1}].`;
                const res = await groq.chat.completions.create({ messages:[{role:'user',content:prompt}], model:'llama-3.3-70b-versatile', temperature:0.4, max_tokens:4096 });
                const text = res.choices[0].message.content; const match = text.match(/\[[\s\S]*\]/);
                if (!match) throw new Error('No valid JSON found');
                lastGenerated = JSON.parse(match[0]).map(q => ({ ...q, type: q.options ? 'mcq' : (q.correct==='True'||q.correct==='False') ? 'truefalse' : 'fillblank', difficulty, points: q.points||1, options: q.options||['','','',''], correct: q.correct||'A' }));
                chatDiv.removeChild(thinking);
                chatDiv.innerHTML += `<div class="ai-message assistant">✅ Generated <strong>${lastGenerated.length}</strong> questions. Click <strong>"Insert Generated Questions"</strong> to add them.</div>`;
                showToast('AI Generated', `${lastGenerated.length} questions created.`, 'success');
            } catch(e) { chatDiv.removeChild(thinking); chatDiv.innerHTML += `<div class="ai-message assistant">❌ ${e.message}</div>`; showToast('AI Error', e.message, 'error'); } finally { hideLoading(); }
        };
        window.insertAIGenerated = () => { if (!lastGenerated.length) { showToast('Nothing', 'Generate questions first.', 'error'); return; } questions = questions.concat(lastGenerated); renderQuestions(); closeAIModal(); showToast('Inserted', `${lastGenerated.length} questions added.`, 'success'); };
        window.openAIModal = () => { document.getElementById('aiModal').classList.add('active'); };
        window.closeAIModal = () => { document.getElementById('aiModal').classList.remove('active'); };
        document.getElementById('useAIBtn').addEventListener('click', () => { if (!currentSubject || !selectedWeeks.length) showToast('Tip', 'Select subject and weeks first for better AI results.', 'info'); openAIModal(); });

        // ==================== CSV UPLOAD ====================
        const bulkModal = document.getElementById('bulkModal'), uploadArea = document.getElementById('uploadArea'), csvFileInput = document.getElementById('csvFile');
        window.openBulkModal = () => bulkModal.classList.add('active');
        window.closeBulkModal = () => { bulkModal.classList.remove('active'); csvData = []; document.getElementById('csvPreview').style.display = 'none'; };
        document.getElementById('bulkUploadBtn').addEventListener('click', openBulkModal);
        document.getElementById('downloadTemplateBtn').addEventListener('click', () => {
            const csv = 'Question,Option A,Option B,Option C,Option D,Correct Answer,Points\n"What is 2+2?",1,2,3,4,C,1\n"Capital of Nigeria?",Lagos,Abuja,Kano,Ibadan,B,2';
            const blob = new Blob([csv],{type:'text/csv'}); const a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = 'question_template.csv'; a.click();
        });
        uploadArea.addEventListener('click', () => csvFileInput.click());
        uploadArea.addEventListener('dragover', e => { e.preventDefault(); uploadArea.style.borderColor = 'var(--accent)'; });
        uploadArea.addEventListener('dragleave', () => { uploadArea.style.borderColor = 'var(--border)'; });
        uploadArea.addEventListener('drop', e => { e.preventDefault(); uploadArea.style.borderColor = 'var(--border)'; if (e.dataTransfer.files[0]) parseCSVFile(e.dataTransfer.files[0]); });
        csvFileInput.addEventListener('change', e => { if (e.target.files[0]) parseCSVFile(e.target.files[0]); });

        function parseCSVFile(file) {
            const reader = new FileReader();
            reader.onload = (e) => {
                const lines = e.target.result.split('\n').filter(l => l.trim());
                csvData = [];
                for (let i = 1; i < lines.length; i++) {
                    const row = lines[i].split(',').map(c => c.trim().replace(/^"|"$/g, ''));
                    if (row.length >= 7 && ['A','B','C','D'].includes(row[5].toUpperCase())) {
                        csvData.push({ question: row[0], options: [row[1],row[2],row[3],row[4]], correct: row[5].toUpperCase(), points: parseInt(row[6])||1, type:'mcq', difficulty:'medium' });
                    }
                }
                document.getElementById('previewTable').innerHTML = '<thead><tr><th>#</th><th>Question</th><th>Correct</th><th>Points</th></tr></thead><tbody>' + csvData.slice(0,10).map((q,i) => `<tr><td>${i+1}</td><td>${escapeHtml(q.question.substring(0,60))}...</td><td>${q.correct}</td><td>${q.points}</td></tr>`).join('') + '</tbody>';
                document.getElementById('csvPreview').style.display = 'block';
            };
            reader.readAsText(file);
        }
        document.getElementById('importCsvBtn').addEventListener('click', () => { if (!csvData.length) { showToast('No Data', 'Upload a valid CSV file first.', 'error'); return; } questions = questions.concat(csvData); renderQuestions(); closeBulkModal(); showToast('Imported', `${csvData.length} questions imported.`, 'success'); });

        // ==================== SAVE / PUBLISH ====================
        async function saveAssessment(status) {
            const title = document.getElementById('title').value.trim();
            if (!currentSubject || !title) { showToast('Required', 'Select a subject and enter a title.', 'error'); return; }
            if (currentType !== 'assignment' && !questions.length) { showToast('No Questions', 'Add at least one question.', 'error'); return; }
            for (let i = 0; i < questions.length; i++) {
                const q = questions[i];
                if (!q.question?.trim()) { showToast('Empty', `Question ${i+1} has no text.`, 'error'); return; }
                if (q.type === 'mcq' && (!q.options || q.options.some(o => !o?.trim()))) { showToast('Empty Options', `Question ${i+1} has empty options.`, 'error'); return; }
                if (q.type !== 'theory' && !q.correct) { showToast('Missing Answer', `Question ${i+1} missing correct answer.`, 'error'); return; }
            }
            showLoading();
            try {
                const data = { teacherId: currentUser.uid, teacherName: `${userData.firstName||''} ${userData.surname||''}`.trim(), type: currentType, subject: currentSubject, title, classId: userData.classTeaching, instructions: document.getElementById('instructions').value, weeksCovered: selectedWeeks, questions, status, createdAt: firebase.firestore.FieldValue.serverTimestamp(), updatedAt: firebase.firestore.FieldValue.serverTimestamp() };
                if (currentType === 'test' || currentType === 'exam') { data.duration = parseInt(document.getElementById('duration')?.value) || 30; data.autoGrade = document.getElementById('autoGrade')?.checked || false; }
                if (currentType === 'assignment') { data.dueDate = document.getElementById('dueDate')?.value; data.totalMarks = parseInt(document.getElementById('totalMarks')?.value) || 20; data.submissionType = document.getElementById('submissionType')?.value || 'text'; }
                if (currentType === 'exam') { data.startDate = document.getElementById('startDate')?.value || null; data.endDate = document.getElementById('endDate')?.value || null; }
                await db.collection('assessments').add(data);
                await db.collection('auditLogs').add({ userId: currentUser.uid, userName: `${userData.firstName||''} ${userData.surname||''}`.trim(), action: status==='published'?'ASSESSMENT_PUBLISHED':'ASSESSMENT_SAVED', details: { type: currentType, title, subject: currentSubject, questionCount: questions.length }, timestamp: firebase.firestore.FieldValue.serverTimestamp() }).catch(()=>{});
                showToast('Success', `Assessment ${status==='published'?'published':'saved'}!`, 'success');
                setTimeout(() => { window.location.href = 'teacher-assessment-history.html'; }, 1500);
            } catch(e) { showToast('Error', e.message, 'error'); } finally { hideLoading(); }
        }
        document.getElementById('saveDraftBtn').addEventListener('click', () => saveAssessment('draft'));
        document.getElementById('publishBtn').addEventListener('click', () => saveAssessment('published'));

        // ==================== AUTH ====================
        auth.onAuthStateChanged(async user => {
            if (!user) { window.location.href = 'teacher-login.html'; return; }
            const doc = await db.collection('users').doc(user.uid).get();
            if (!doc.exists || doc.data().role !== 'teacher') { auth.signOut(); window.location.href = 'index.html'; return; }
            currentUser = user; userData = doc.data();
            const pic = document.getElementById('profilePic');
            if (userData.photoURL) pic.innerHTML = `<img src="${userData.photoURL}" alt="Profile">`;
            else pic.textContent = (userData.firstName?.[0]||'T')+(userData.surname?.[0]||'E');
            subjects = userData.subjects || [];
            const sel = document.getElementById('subject');
            sel.innerHTML = '<option value="">Select a subject...</option>';
            subjects.forEach(s => { const opt = document.createElement('option'); opt.value = s; opt.textContent = s; sel.appendChild(opt); });
            try { await remoteConfig.fetchAndActivate(); const key = remoteConfig.getString('groq_api_key'); if (key) groq = new Groq({ apiKey: key, dangerouslyAllowBrowser: true }); } catch(e) {}
            renderDynamicSettings();
        });
        document.getElementById('logoutBtn').addEventListener('click', () => auth.signOut().then(() => window.location.href = 'index.html'));

        // Expose globally
        window.setCorrect = setCorrect; window.removeQuestion = removeQuestion; window.editQuestion = editQuestion;
        window.toggleWeek = toggleWeek; window.generateFromLessonNotes = generateFromLessonNotes;
        window.insertAIGenerated = insertAIGenerated; window.openAIModal = openAIModal;
        window.closeAIModal = closeAIModal; window.openBulkModal = openBulkModal; window.closeBulkModal = closeBulkModal;
    </script>
</body>
</html>
